#include <stdio.h>

void main(){
	FILE* stream;
        char* line = NULL;
        size_t len = 0;
        ssize_t read;


	stream = fopen("simple.bin", "rb");
        if(stream == NULL)
                exit(EXIT_FAILURE);

	
	



return ;}
